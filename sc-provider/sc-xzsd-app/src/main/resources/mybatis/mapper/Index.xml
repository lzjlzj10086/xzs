<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >

<mapper namespace="com.xzsd.app.index.dao.IndexDao">
    <select id="imageGroupList" resultType="com.xzsd.app.index.entity.ImageGroup">
        select
            image_url imageUrl,
            goods_code goodsCode
        from t_imagegroup
        where is_deleted = 0
        order by sort asc
    </select>
    <select id="hotGoodsListByPage" resultType="com.xzsd.app.index.entity.HotGoods">
        select
            goods_code goodsCode,
            goods_name goodsName,
            goods_price goodsPrice,
            goods_image goodsImage
        from t_hot
        where is_deleted = 0
        order by hot_sort asc
    </select>
</mapper>